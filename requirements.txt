# SkyHigh Scanner — Dependencies
# All dependencies are optional; the scanner degrades gracefully without them.
# Install only what you need for your target types.

# Core (recommended for all)
requests>=2.28.0            # HTTP transport, NVD API sync, web server scanning

# Linux scanning
paramiko>=3.0.0             # SSH connections for Linux hosts

# Cisco scanning
netmiko>=4.0.0              # SSH for Cisco IOS/IOS-XE/NX-OS devices
pysnmp-lextudio>=5.0.0      # SNMP for Cisco device discovery

# Windows scanning
pywinrm>=0.4.3              # WinRM connections for Windows hosts
# impacket>=0.11.0          # SMB/WMI (alternative to WinRM) — optional

# PDF report generation
# weasyprint>=60.0          # HTML-to-PDF conversion for --pdf flag
